#include "http_request.h"

namespace natsu {
namespace http {


/*
std::string HttpRequest::header(const std::string& k)
{
    auto it = header_.find(k)
    if( it != header_.end())
    {
        return it->second;
    }

    return "";
}

std::map<std::string,std::string> HttpRequest::query()
{
    std::map<std::string,std::string> q;
    return q;
}*/


}}
